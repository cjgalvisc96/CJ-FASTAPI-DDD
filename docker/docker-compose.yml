name: cj-fastapi-ddd

# Host ports are OFFSET from the defaults (5432/6379/8080/8000/4566) so this stack can run alongside
# sibling projects (e.g. CJ-DJANGO-MVC) without port conflicts. Container-internal ports are standard —
# services reach each other by name over `ddd-net`, so only host-published ports change.

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ${DB_NAME:-ddd}
      POSTGRES_USER: ${DB_USER:-ddd}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-ddd}
    ports:
      - "${POSTGRES_HOST_PORT:-15432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - ddd-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-ddd}"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7
    ports:
      - "${REDIS_HOST_PORT:-16379}:6379"
    networks:
      - ddd-net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  keycloak:
    image: quay.io/keycloak/keycloak:26.0
    command: ["start-dev", "--import-realm"]
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: admin
      KC_BOOTSTRAP_ADMIN_PASSWORD: admin
      KC_HTTP_PORT: 8080
    ports:
      - "${KEYCLOAK_HOST_PORT:-18080}:8080"
    volumes:
      - ./keycloak/realm-export.json:/opt/keycloak/data/import/realm-export.json:ro
    networks:
      - ddd-net
    healthcheck:
      test:
        ["CMD-SHELL", "exec 3<>/dev/tcp/localhost/8080 && echo ok >&3 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  # One-shot Atlas migration; the app waits for it to complete before starting.
  atlas:
    image: arigaio/atlas:latest
    command: >
      migrate apply
      --url "postgres://ddd:ddd@postgres:5432/ddd?sslmode=disable"
      --dir "file:///migrations/versions"
    volumes:
      - ../migrations:/migrations:ro
    networks:
      - ddd-net
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"

  app:
    build:
      context: ..
      dockerfile: Dockerfile
      target: dev
    image: local/cj-fastapi-ddd:dev
    command: sh -c "ddd-seed && ddd-api"
    # All app config comes from .env; the values below only override the service hostnames so the app
    # reaches the sibling containers by name. (For host-based runs, .env already points at localhost.)
    env_file:
      - ../.env
    environment:
      # Container-network overrides (service names + standard container ports), independent of the
      # offset host ports above. This is why the app in-container always uses 5432/6379/8080.
      DB_HOST: postgres
      DB_PORT: "5432"
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      KEYCLOAK_URL: http://keycloak:8080
      API_PORT: "8000"   # container listens on 8000; host maps it to APP_HOST_PORT (18000)
    ports:
      - "${APP_HOST_PORT:-18000}:8000"
    volumes:
      - ../src:/app/src:ro
    networks:
      - ddd-net
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      atlas:
        condition: service_completed_successfully

  # Local AWS emulator for exercising the Terraform/Terragrunt stack offline.
  # Start on demand (docker compose --profile infra up -d floci, or task floci:up); target it with
  # task terragrunt:plan ENV=local.
  floci:
    image: floci/floci:latest
    profiles: ["infra"]
    environment:
      FLOCI_HOSTNAME: floci
    ports:
      - "${FLOCI_HOST_PORT:-14566}:4566"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock   # floci spawns backing containers
      - flocidata:/var/lib/floci
    networks:
      - ddd-net
    healthcheck:
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/localhost/4566 && echo ok >&3 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 15s

networks:
  ddd-net:
    name: ddd-net
    driver: bridge

volumes:
  pgdata:
  flocidata:
